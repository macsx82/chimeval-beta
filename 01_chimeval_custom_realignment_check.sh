#!/usr/bin/env bash

#script to perform a check to determine if there is the need of a custom realignment


#We just need to check the existence of the custom realigned file, and, if needed start the realignemnt of the sample